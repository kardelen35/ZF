import { MetricTypePipe } from './metric-type.pipe';

describe('MetricTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MetricTypePipe();
    expect(pipe).toBeTruthy();
  });
});
