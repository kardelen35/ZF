import { WireVersionPipe } from './wire-version.pipe';

describe('WireVersionPipe', () => {
  it('create an instance', () => {
    const pipe = new WireVersionPipe();
    expect(pipe).toBeTruthy();
  });
});
